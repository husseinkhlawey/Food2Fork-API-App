node version: 6.11.2
Test OS: Windows and Linux

PLEASE NOTE: FOOD2FORK API has shut down in 2019.

Installation:

After downloading you will need to install the required dependencies via the command "npm install".

Launching and Testing:

To launch the app simply navigate inside it's folder in terminal and execute "node server.js".

Afterwords visit "localhost:3000" to test the app.
